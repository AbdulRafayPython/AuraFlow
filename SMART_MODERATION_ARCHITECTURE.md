# Smart Moderation Agent - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         SMART MODERATION AGENT                          │
│                         Real-time Integration                           │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND                                    │
└─────────────────────────────────────────────────────────────────────────┘

    User Types Message
           │
           ▼
    ┌─────────────┐
    │  Dashboard  │ ──────┐
    │   (Chat)    │       │
    └─────────────┘       │
           │              │
           │              │ Send via Socket.IO
           ▼              │
    socket.emit()         │
    'new_message' ────────┘
           │
           │
           ▼
    ┌─────────────────────────────────────────────────┐
    │              Socket Connection                   │
    │          (WebSocket Transport)                   │
    └─────────────────────────────────────────────────┘
           │
           │
           ▼

┌─────────────────────────────────────────────────────────────────────────┐
│                              BACKEND                                     │
└─────────────────────────────────────────────────────────────────────────┘

    routes/sockets.py
           │
           ▼
    @socketio.on('new_message')
    def on_new_message(data):
           │
           ▼
    ┌─────────────────────────────────────┐
    │   ModerationAgent.moderate_message()│
    │   (agents/moderation.py)             │
    └─────────────────────────────────────┘
           │
           │
    ┌──────┴─────────────────────────────┐
    │  Lexicon Analysis                  │
    │  (moderation_keywords.json)        │
    │                                    │
    │  • Check profanity (EN + RU)       │
    │  • Check hate speech               │
    │  • Check harassment                │
    │  • Check spam patterns             │
    │  • Check personal info             │
    │  • Calculate scores                │
    └────────────┬───────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────┐
    │  Determine Action                  │
    │                                    │
    │  Score >= 0.9  → BLOCK (high)      │
    │  Score >= 0.6  → FLAG (medium)     │
    │  Score >= 0.3  → WARN (low)        │
    │  Score < 0.3   → ALLOW (none)      │
    └────────────┬───────────────────────┘
                 │
    ┌────────────┴───────────────────────────────────────┐
    │                                                     │
    ▼                     ▼                  ▼           ▼
┌──────────┐      ┌──────────┐      ┌──────────┐  ┌──────────┐
│  ALLOW   │      │   WARN   │      │   FLAG   │  │  BLOCK   │
└──────────┘      └──────────┘      └──────────┘  └──────────┘
    │                  │                  │             │
    │                  │                  │             │
    ▼                  ▼                  ▼             ▼
Broadcast        Broadcast          Broadcast      DON'T broadcast
to channel       to channel         to channel     to channel
    │                  │                  │             │
    │                  ▼                  │             │
    │          emit('moderation_         │             │
    │           warning')                 │             │
    │           to sender                 │             │
    │                                     ▼             ▼
    │                             emit('moderation_  emit('message_
    │                              alert')           blocked')
    │                              to moderators     to sender only
    │                                     │             │
    │                                     ▼             ▼
    │                             emit('moderation_alert')
    │                             to moderators
    │
    └─────────────────────┬───────────────┬─────────────┘
                          │               │
                          ▼               ▼
                  ┌─────────────────────────────┐
                  │  Log to ai_agent_logs       │
                  │  (Database)                 │
                  │                             │
                  │  • user_id                  │
                  │  • channel_id               │
                  │  • action_type: moderation  │
                  │  • input_text: message      │
                  │  • output_text: JSON result │
                  │  • confidence_score         │
                  └─────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         SOCKET EVENTS TO FRONTEND                       │
└─────────────────────────────────────────────────────────────────────────┘

    socket.on('message_received')  ──────────────┐
           │                                     │
           ▼                                     │
    ┌─────────────────┐                         │
    │  Dashboard      │                         │
    │  Message List   │                         │
    │                 │                         │
    │  Display with   │◄────────────────────────┘
    │  ModerationBadge│
    │  (if flagged)   │
    └─────────────────┘

    socket.on('message_blocked')
           │
           ▼
    ┌─────────────────┐
    │ ModerationToast │
    │                 │
    │ 🔴 Red Toast    │
    │ "Message        │
    │  Blocked"       │
    └─────────────────┘

    socket.on('moderation_warning')
           │
           ▼
    ┌─────────────────┐
    │ ModerationToast │
    │                 │
    │ 🟡 Yellow Toast │
    │ "Content        │
    │  Warning"       │
    └─────────────────┘

    socket.on('moderation_alert')
           │
           ▼
    ┌─────────────────┐
    │ ModerationToast │
    │ (Moderators)    │
    │                 │
    │ 🟠 Orange Toast │
    │ "Moderation     │
    │  Alert"         │
    └─────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         MODERATION CATEGORIES                           │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│  ✅ CLEAN (action: allow, severity: none)                           │
│                                                                      │
│  • Normal conversation                                               │
│  • Constructive feedback                                             │
│  • Polite disagreement                                               │
│  • No violations detected                                            │
│                                                                      │
│  Frontend: Message displays normally, no badge                       │
│  Backend: Broadcast via 'message_received', no logging               │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│  ⚠️ SUSPICIOUS/WARN (action: warn, severity: low)                   │
│                                                                      │
│  • Mild profanity (damn, hell, crap)                                 │
│  • Borderline slang                                                  │
│  • Passive-aggressive tone                                           │
│  • Score: 0.3 - 0.59                                                 │
│                                                                      │
│  Frontend: Yellow toast to sender, message visible                   │
│  Backend: Broadcast + emit 'moderation_warning', log to DB           │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│  🚩 FLAGGED (action: flag, severity: medium)                        │
│                                                                      │
│  • Direct insults (idiot, loser)                                     │
│  • Harassment (chup kar, shut up)                                    │
│  • Spam (repeated chars, caps, emojis)                               │
│  • Personal information (phone, email)                               │
│  • Score: 0.6 - 0.89                                                 │
│                                                                      │
│  Frontend: Orange badge, message visible, moderator alert            │
│  Backend: Broadcast + emit 'moderation_alert' to moderators, log     │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│  🚫 TOXIC/BLOCK (action: block, severity: high)                     │
│                                                                      │
│  • Hate speech (terrorist, scum)                                     │
│  • Severe profanity (fuck, bitch, bhenchod)                          │
│  • Threats (kill, die, maar doon ga)                                 │
│  • Multiple violations                                               │
│  • Repeat offenders (3+ violations in 24h)                           │
│  • Score: 0.9+                                                       │
│                                                                      │
│  Frontend: Red toast to sender only, message NOT visible             │
│  Backend: DON'T broadcast, emit 'message_blocked' to sender,         │
│           emit 'moderation_alert' to moderators, log to DB           │
└──────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         LEXICON STRUCTURE                               │
└─────────────────────────────────────────────────────────────────────────┘

moderation_keywords.json
│
├── profanity
│   ├── english: [damn, hell, shit, fuck, ass, bitch, ...]
│   └── roman_urdu: [harami, kutta, gandu, chutiya, bhenchod, ...]
│
├── hate_speech
│   ├── english: [hate, kill, terrorist, scum, ...]
│   └── roman_urdu: [nafrat, maro, kafir, ...]
│
├── harassment
│   ├── english: [shut up, idiot, loser, ugly, ...]
│   └── roman_urdu: [chup kar, bewakoof, pagal, nikal, ...]
│
├── spam_patterns
│   ├── repeated_characters: "(.)\\1{4,}"
│   ├── excessive_caps: "[A-Z]{10,}"
│   ├── excessive_emojis: threshold: 10
│   └── link_spam: max_links: 3
│
├── personal_info_patterns
│   ├── phone: regex for phone numbers
│   ├── email: regex for emails
│   └── credit_card: regex for card numbers
│
└── severity_levels
    ├── low: 0.3 → warn
    ├── medium: 0.6 → flag
    └── high: 0.9 → block

┌─────────────────────────────────────────────────────────────────────────┐
│                         USER VIOLATION TRACKING                         │
└─────────────────────────────────────────────────────────────────────────┘

Time: T0 (First violation)
    User sends: "damn"
    Action: WARN (low severity)
    Violations: 1

Time: T0 + 10 min (Second violation)
    User sends: "shit"
    Action: WARN (low severity)
    Violations: 2

Time: T0 + 20 min (Third violation)
    User sends: "hell"
    Action: WARN (low severity)
    Violations: 3

Time: T0 + 30 min (Fourth violation - ESCALATION)
    User sends: "crap"
    Normal Score: 0.3 (would be WARN)
    Violation Count: 3+ in 24 hours
    ESCALATED TO: BLOCK (high severity)
    Reason: ['profanity', 'repeat_offender']

Time: T0 + 25 hours (Violations expired)
    Violations in last 24h: 0
    Next message will be judged normally

┌─────────────────────────────────────────────────────────────────────────┐
│                         TESTING MATRIX                                  │
└─────────────────────────────────────────────────────────────────────────┘

Test Case                    | Input                            | Expected
─────────────────────────────┼──────────────────────────────────┼─────────
1. Clean                     | "Hello world"                    | ALLOW
2. Mild Profanity (EN)       | "This is damn good"              | WARN
3. Profanity (RU)            | "Ye kya bakwas hai"              | WARN/FLAG
4. Hate Speech (EN)          | "You terrorist scum"             | BLOCK
5. Harassment (EN)           | "Shut up idiot"                  | FLAG
6. Harassment (RU)           | "Chup kar bewakoof"              | FLAG
7. Spam (Repeated)           | "HELLOOOOOOO!!!!!!"              | WARN/FLAG
8. Spam (Caps)               | "THIS IS ALL CAPS"               | WARN/FLAG
9. Spam (Emojis)             | "😀😀😀😀😀😀😀😀😀😀😀"              | FLAG
10. Personal Info (Phone)    | "+92-300-1234567"                | FLAG
11. Personal Info (Email)    | "test@example.com"               | FLAG
12. Multiple Violations      | "You fucking idiot, die"         | BLOCK
13. Repeat Offender (4th)    | "damn" (after 3 violations)      | BLOCK

┌─────────────────────────────────────────────────────────────────────────┐
│                         COMPONENT HIERARCHY                             │
└─────────────────────────────────────────────────────────────────────────┘

App.tsx
  └── BrowserRouter
      ├── ModerationToastListener  ← Listens to socket events globally
      └── Routes
          └── Dashboard
              └── Messages List
                  └── Message Item
                      ├── Author Name
                      ├── Timestamp
                      ├── ModerationBadge ← Shows for flagged messages
                      └── Content

ModerationToastListener (invisible)
  └── socket.on('message_blocked')
  └── socket.on('moderation_warning')
  └── socket.on('moderation_alert')
      └── toast({ ... })  ← Shows notification

┌─────────────────────────────────────────────────────────────────────────┐
│                         FILE DEPENDENCIES                               │
└─────────────────────────────────────────────────────────────────────────┘

Backend:
  app.py
    └── routes/sockets.py
        └── agents/moderation.py
            └── lexicons/moderation_keywords.json
                └── database.py (ai_agent_logs table)

  routes/messages.py
    └── agents/moderation.py
        └── (same as above)

Frontend:
  App.tsx
    └── ModerationToastListener.tsx
        └── socket.ts
            └── Backend Socket.IO server

  Dashboard.tsx
    └── ModerationBadge.tsx
        └── Message interface (types/index.ts)

  types/index.ts
    └── Message interface with moderation field

```
